<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.softsociety.exam.dao.MemberDAO">

<!-- 회원정보 저장 -->
<insert id="joinMember" parameterType="Member">
	INSERT INTO market_member (memberid, memberpw, membername, phone)
	VALUES (#{memberid}, #{memberpw}, #{membername}, #{phone})
</insert>
<!-- ID 개수 확인 -->
<select id="countMemberid" parameterType="String" resultType="int">
	select count(*) from market_member where memberid = #{memberid}
</select>
</mapper>
